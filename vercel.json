{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "prisma generate && next build"
      }
    }
  ],
  "env": {
    "PRISMA_SKIP_MIGRATE_DEPLOY": "true"
  }
} 